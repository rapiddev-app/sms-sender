# Техническое видение: Авто рассылка СМС

## Стек технологий

| Компонент | Технология | Версия |
|---|---|---|
| Язык | Python | 3.11+ |
| GUI-фреймворк | CustomTkinter | latest |
| Чтение Excel | openpyxl | latest |
| ADB-интеграция | pure-python-adb / системный adb.exe | latest |
| Пакетный менеджер | uv | latest |
| Сборка в .exe | PyInstaller | latest |

## Архитектура

Монолитный desktop-инструмент с разделением на три слоя внутри одного приложения.

```
┌─────────────────────────────────────────┐
│           GUI (CustomTkinter)           │
│  Экран 1: Загрузка Excel + валидация    │
│  Экран 2: Конструктор шаблона           │
│  Экран 3: Настройки отправки            │
│  Экран 4: Прогресс рассылки             │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│           Бизнес-логика                 │
│  • ExcelLoader — чтение и валидация     │
│  • MessageBuilder — сборка текста       │
│  • SendQueue — очередь с задержками     │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│           ADB-слой                      │
│  • DeviceManager — обнаружение телефона │
│  • SmsSender — отправка команд          │
│  • StatusReader — чтение статусов       │
└─────────────────────────────────────────┘
```

Никаких серверов, баз данных и сетевых запросов — всё работает локально. Распространяется как единый `.exe`-файл без установки Python.

## Структура проекта

```
SMS auto/
├── doc/                        # Документация проекта
├── src/
│   ├── main.py                 # Точка входа, запуск GUI
│   ├── app.py                  # Главный класс приложения (навигация между экранами)
│   │
│   ├── ui/                     # GUI-экраны
│   │   ├── screen_import.py    # Экран 1: Загрузка Excel
│   │   ├── screen_builder.py   # Экран 2: Конструктор шаблона
│   │   ├── screen_settings.py  # Экран 3: Настройки отправки
│   │   └── screen_sending.py   # Экран 4: Прогресс рассылки
│   │
│   ├── core/                   # Бизнес-логика
│   │   ├── excel_loader.py     # Чтение и валидация Excel
│   │   ├── message_builder.py  # Сборка текста из шаблона
│   │   └── send_queue.py       # Очередь отправки с задержками
│   │
│   └── adb/                    # ADB-интеграция
│       ├── device_manager.py   # Обнаружение подключённого телефона
│       ├── sms_sender.py       # Отправка SMS через ADB
│       └── status_reader.py    # Чтение статуса доставки
│
├── tests/                      # Юнит-тесты для core/
├── assets/                     # Иконки, шрифты
├── pyproject.toml              # Зависимости (uv)
├── build.py                    # Скрипт сборки .exe через PyInstaller
└── .gitignore
```

## UI-экраны (Wizard)

| Экран | Название | Ключевые элементы |
|---|---|---|
| **1** | Загрузка базы | Кнопка «Открыть Excel», таблица-превью, индикатор ошибок валидации номеров |
| **2** | Конструктор шаблона | Поле основного текста, метка `{переменная}`, счётчик символов (160 / 70 для кириллицы), блок предпросмотра |
| **3** | Настройки отправки | Размер группы, задержка между SMS (сек), задержка между группами (сек), индикатор подключения ADB |
| **4** | Рассылка | Прогресс-бар, лог строк (номер → статус ⏳/✅/❌), кнопки «Пауза»/«Стоп», итоговая статистика |

## Инфраструктура и деплой

- **Среда:** локальный Windows ПК пользователя
- **Распространение:** единый `.exe`-файл, собранный через PyInstaller. **Никаких предустановок не требуется** — Python-рантайм и все зависимости упакованы внутрь.
- **ADB:** `adb.exe` + `AdbWinApi.dll` + `AdbWinUsbApi.dll` хранятся в `bin/adb/` и включаются в бандл PyInstaller. Приложение ищет `adb.exe` через `sys._MEIPASS` (PyInstaller temp dir) с fallback на `bin/adb/` рядом с `.exe`. Пользователю **не нужно** устанавливать Android Platform Tools или добавлять что-либо в PATH.
- **Требования к пользователю:** только включённый **режим разработчика** и **USB-отладка** на Android-телефоне — это аппаратное требование, которое нельзя обойти программно.
- **Обновления:** ручная замена `.exe`-файла
