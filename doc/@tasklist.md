# Tasklist: Авто рассылка СМС

## Текущий статус
- Итерация: 2 / 4
- Прогресс: 14 / 30 задач (47%)

---

## В работе

### Iteration 1: Ядро — загрузка и валидация данных
- [x] `core/excel_loader.py` — чтение `.xlsx`, извлечение двух столбцов (номер + переменная)
- [x] Валидация номеров: проверка формата (10-11 цифр, поддержка `7xxx`, `8xxx`, `+7xxx`)
- [x] Возврат структурированного списка контактов + списка ошибок валидации
- [x] `core/message_builder.py` — подстановка переменной в шаблон, подсчёт символов
- [x] Юнит-тесты: `tests/test_excel_loader.py`
- [x] Юнит-тесты: `tests/test_message_builder.py`

> **Результат:** модули `core/` полностью покрыты тестами и работают независимо от GUI.

---

### Iteration 2: ADB-слой — работа с телефоном
- [x] Скачать и положить `adb.exe` + `AdbWinApi.dll` + `AdbWinUsbApi.dll` в `bin/adb/` (через `scripts/fetch_adb.py`)
- [x] `adb/device_manager.py` — поиск подключённых устройств, статус (подключён / не найден)
- [ ] `adb/sms_sender.py` — отправка SMS через ADB-команду (`am start` с ACTION_SENDTO)
- [ ] `adb/status_reader.py` — чтение статуса отправленных SMS из content provider телефона
- [ ] `core/send_queue.py` — очередь отправки: группы + задержки, поддержка паузы и остановки

> **Результат:** из Python-консоли можно отправить тестовое SMS через подключённый телефон.

---

### Iteration 3: GUI — Wizard (4 экрана)
- [ ] `app.py` — навигация между экранами, передача состояния между шагами
- [ ] `ui/screen_import.py` — Экран 1: открытие файла, таблица-превью, индикаторы ошибок, кнопка «Далее»
- [ ] `ui/screen_builder.py` — Экран 2: поле текста, вставка `{переменная}`, счётчик символов, предпросмотр
- [ ] `ui/screen_settings.py` — Экран 3: размер группы, задержки, статус ADB, кнопка «Начать рассылку»
- [ ] `ui/screen_sending.py` — Экран 4: прогресс-бар, лог ⏳/✅/❌, кнопки «Пауза»/«Стоп», статистика

> **Результат:** полностью рабочий MVP — от загрузки Excel до завершения рассылки.

---

### Iteration 4: Полировка, экспорт и сборка .exe
- [ ] Добавить иконку приложения в `assets/`
- [ ] **Сохранение шаблонов** — сохранение/загрузка именованных шаблонов в JSON-файл, список на Экране 2
- [ ] **Экспорт результатов** — кнопка «Выгрузить Excel» со столбцом `Статус` (✅/❌/⏳)
- [ ] `core/report_exporter.py` — запись статусов обратно в `.xlsx` через `openpyxl`
- [ ] Обработка граничных случаев: телефон отключился, пустой Excel, некорректный шаблон
- [ ] `build.py` — скрипт сборки PyInstaller (с `bin/adb/` и `assets/` в бандле)
- [ ] Финальный `README.md` — инструкция для пользователя
- [ ] Smoke-тест: собранный `.exe` проходит все 4 экрана

> **Результат:** готовый `.exe` — загрузил Excel → отправил → выгрузил с отметками.

---

## Выполнено

### Iteration 0: Скелет проекта — завершено 2026-04-29
- [x] Инициализация окружения: `uv init`, `pyproject.toml` с зависимостями
- [x] Установка зависимостей: `customtkinter`, `openpyxl`, `pytest`, `ruff`
- [x] Создание физической структуры каталогов (`src/ui/`, `src/core/`, `src/adb/`, `tests/`, `bin/adb/`, `assets/`)
- [x] Точка входа `main.py` — запускает пустое окно приложения
- [x] Конфигурация `ruff` в `pyproject.toml`
- [x] `README.md` с инструкцией по запуску

> **Результат:** `python src/main.py` открывает пустое окно приложения.
