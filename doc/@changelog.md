# Авто рассылка СМС - История изменений (Changelog)

## [2026-04-29] — Iteration 4: Экспорт результатов

### Добавлено
- `src/core/report_exporter.py` — экспорт копии исходного `.xlsx` со столбцом
  `Статус`
- Кнопка `Выгрузить Excel` на экране рассылки после остановки или завершения
  процесса
- `tests/test_report_exporter.py` — unit-тесты добавления/переиспользования
  столбца `Статус` и обработки ошибок чтения Excel

## [2026-04-29] — Iteration 4: Сохранение шаблонов

### Добавлено
- `src/core/template_store.py` — локальное JSON-хранилище именованных SMS-шаблонов
  в `%APPDATA%/SMS Auto/templates.json`
- Управление шаблонами на экране конструктора: имя, список, сохранение, загрузка
  и удаление
- `tests/test_template_store.py` — unit-тесты хранения, перезаписи, удаления,
  валидации имени и обработки повреждённого JSON

## [2026-04-29] — Iteration 4: Иконка приложения

### Добавлено
- `assets/icon.ico` — иконка desktop-приложения
- Подключение иконки к главному окну `CustomTkinter` с поддержкой dev-режима и
  будущего PyInstaller-бандла
- `tests/test_app_icon.py` — unit-тесты поиска ресурса и безопасной установки иконки

## [2026-04-29] — Iteration 3: Wizard-навигация

### Добавлено
- `src/app.py` — каркас wizard-интерфейса на 4 шага: загрузка базы,
  конструктор шаблона, настройки и рассылка
- `WizardState` — общее состояние между будущими UI-экранами: файл Excel,
  контакты, ошибки импорта, шаблон и параметры отправки
- Верхняя навигация по шагам и нижние кнопки `Назад` / `Далее` с блокировкой
  на первом и последнем шаге

## [2026-04-29] — Iteration 3: Экран импорта Excel

### Добавлено
- `src/ui/screen_import.py` — экран выбора `.xlsx`, загрузки контактов через
  `core.excel_loader`, превью валидных строк и список ошибок валидации
- Блокировка кнопки `Далее` на первом шаге до появления валидных контактов
- `tests/test_screen_import.py` — unit-тесты форматирования ошибок импорта

## [2026-04-29] — Iteration 3: Экран конструктора шаблона

### Добавлено
- `src/ui/screen_builder.py` — экран ввода SMS-шаблона, кнопка вставки
  `{переменная}`, счётчик символов/сегментов и предпросмотр по первому контакту
- Блокировка кнопки `Далее` на втором шаге до заполнения шаблона
- `tests/test_screen_builder.py` — unit-тесты проверки готовности шаблона

## [2026-04-29] — Iteration 3: Экран настроек отправки

### Добавлено
- `src/ui/screen_settings.py` — экран настройки размера группы, задержек между SMS
  и группами, проверки ADB/Android-компаньона и перехода к старту рассылки
- Валидация числовых настроек с поддержкой дробей через точку и запятую
- `tests/test_screen_settings.py` — unit-тесты парсинга и валидации настроек

## [2026-04-29] — Iteration 3: Экран рассылки

### Добавлено
- `src/ui/screen_sending.py` — экран выполнения рассылки с progress-bar,
  статистикой, логом отправки и кнопками `Пауза` / `Продолжить` / `Стоп`
- Запуск `SendQueue` в фоновом потоке, чтобы GUI не зависал во время отправки
- Поддержка редактирования шаблона на паузе: после `Пауза` можно вернуться на
  экран конструктора, изменить текст и продолжить с места остановки
- `tests/test_screen_sending.py` — unit-тесты расчёта прогресса и итоговой статистики

### Изменено
- `core/send_queue.py` умеет обновлять шаблон для ещё не начатых отправок
- Вкладки wizard закреплены как навигация: повторный клик по `Рассылка` не
  перезапускает очередь
- `Стоп` завершает процесс как финальное состояние, готовое к будущей выгрузке
  результата

### Исправлено
- Экранирование текста SMS при передаче через `adb shell am broadcast`: ссылки,
  переносы строк и спецсимволы больше не интерпретируются Android shell как
  отдельные команды

## [2026-04-29] — Iteration 2: Очередь отправки SMS

### Добавлено
- `src/core/send_queue.py` — синхронная очередь отправки с настройками размера
  группы, задержек между SMS и группами, событиями прогресса для GUI, поддержкой
  `pause()`, `resume()` и `stop()`
- `tests/test_send_queue.py` — unit-тесты очереди: порядок отправки, подстановка
  переменной, групповые задержки, обработка ошибки отправки, остановка и
  валидация настроек

## [2026-04-29] — Iteration 2: Android-компаньон для отправки SMS

### Добавлено
- `android/sms_companion/` — минимальное Android-приложение-компаньон на Java:
  экран выдачи разрешения `SEND_SMS`, приём ADB-команд через broadcast,
  отправка SMS через `SmsManager`, фиксация статусов в JSONL-журнал
- `src/adb/companion.py` — общие ADB-утилиты для работы с Android-компаньоном
- `src/adb/sms_sender.py` — установка APK, открытие экрана разрешений,
  чтение auth token и отправка SMS-команд в Android-компаньон
- `src/adb/status_reader.py` — чтение статусов отправки из приватного журнала
  Android-компаньона через `adb exec-out run-as`
- `tests/test_sms_sender.py`, `tests/test_status_reader.py` — unit-тесты ADB-клиента
  без реального запуска `adb.exe`
- CLI-команда `sms-auto-install-companion` для установки APK и открытия экрана
  выдачи разрешений
- ADR-0004: собственный Android-компаньон вместо автоматизации чужого SMS-приложения

### Изменено
- План Iteration 2 переведён с `ACTION_SENDTO` на контролируемую отправку через
  Android-компаньон
- Android-сборка зафиксирована на совместимой паре AGP `8.12.0` + Gradle `8.13`

## [2026-04-29] — Iteration 2 (in progress): ADB device_manager

### Добавлено
- `src/adb/device_manager.py` — поиск подключённых Android-устройств через
  `subprocess` + системный `adb.exe`. Публичные API: `find_adb_executable`,
  `list_devices`, `get_connection_status`. Парсер `_parse_devices_output`
  устойчив к шумным строкам adb-демона и метаданным `-l`. Все ошибки
  оборачиваются в `AdbNotFoundError` / `AdbError`, высокоуровневая функция
  `get_connection_status` агрегирует их в 7 состояний для GUI
  (`READY`, `NO_DEVICE`, `UNAUTHORIZED`, `OFFLINE`, `MULTIPLE_DEVICES`,
  `ADB_NOT_FOUND`, `ADB_ERROR`)
- `tests/test_device_manager.py` — 22 unit-теста: парсер вывода, обёртка
  subprocess (моки timeout/exit code/OSError), агрегатор статусов, поиск
  `adb.exe` (через подмену `_PROJECT_ROOT` и `sys._MEIPASS`)
- ADR-0003: прямой вызов `adb.exe` вместо `pure-python-adb`

## [2026-04-29] — Iteration 1 (in progress): Загрузчик Excel

### Добавлено
- `src/core/models.py` — dataclass-модели `Contact`, `ValidationError`, `LoadResult`
  (вынесены отдельно от загрузчика для переиспользования в `message_builder`,
  `send_queue`, UI-слое без риска циклических импортов)
- `src/core/excel_loader.py` — чтение `.xlsx` через openpyxl (read_only режим),
  нормализация номеров к формату `+7xxxxxxxxxx`, обнаружение дубликатов,
  пустых полей и невалидных строк. Системные ошибки бросают исключения
  (`FileNotFoundError`, `InvalidExcelFormatError`), доменные — попадают в
  `LoadResult.errors`
- `tests/conftest.py` — фабрика временных `.xlsx` через openpyxl
- `tests/test_excel_loader.py` — 24 unit-теста (happy path, валидация,
  дубликаты, нормализация, числовые значения из Excel, системные ошибки)
- `tests/fixtures/test_contacts.xlsx` — фикстура для ручной проверки
- В `.gitignore` добавлены: `.ruff_cache/`, `.pytest_cache/`, `.claude/`,
  `/test_contacts.xlsx` (корневая ad-hoc копия)

## [2026-04-29] — Iteration 0: Скелет проекта

### Добавлено
- Инициализация окружения через `uv`, `pyproject.toml` с зависимостями (`customtkinter`, `openpyxl`, `pytest`, `ruff`, `pyinstaller`)
- Физическая структура каталогов: `src/{ui,core,adb}/`, `tests/`, `bin/adb/`, `assets/`
- Точка входа `src/main.py` + класс `SMSAutoApp` в `src/app.py` — открывает пустое окно 960×680 с центровкой
- Конфигурация `ruff` в `pyproject.toml` (line-length 100, правила `E,F,W,I`)
- `README.md` с инструкцией по dev-запуску, тестам, линтеру и сборке `.exe`
